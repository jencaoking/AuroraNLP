# AuroraNLP Dependencies
# ======================
# 核心依赖: 无 (纯 Python 标准库实现)
# Core dependencies: None (Pure Python standard library implementation)

# 开发依赖 (Development dependencies)
# ----------------------------------
pytest>=7.0.0

# 可选依赖 (Optional dependencies)
# --------------------------------
# 以下依赖用于增强功能，非必需
# The following dependencies are optional for enhanced features

# 类型检查 (Type checking)
# mypy>=1.0.0

# 代码格式化 (Code formatting)
# black>=23.0.0
# isort>=5.12.0

# 代码检查 (Linting)
# ruff>=0.1.0

# 性能测试 (Performance testing)
# pytest-benchmark>=4.0.0

# 覆盖率 (Coverage)
# pytest-cov>=4.0.0

# 深度学习依赖 (Deep Learning dependencies)
# ----------------------------------------
# 以下依赖用于 BiLSTM-CRF 模型
# The following dependencies are for BiLSTM-CRF model

# PyTorch
# torch>=2.0.0

# CRF 实现
# torchcrf>=1.1.1
